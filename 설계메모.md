# 설계 메모

## 최우선 문제

채널별 CSV의 컬럼명·날짜 형식·지표 정의 차이로 인해 매주 반복되는 수동 정리와 계산 작업. 수집 이후 단계인 정규화·파생 지표·전주 비교는 규칙이 명확하고 자동화 효과가 큰 영역이므로 최우선 대상으로 선정.

## 해결 구조

`원본 CSV → 헤더 기반 채널 판별 → 공통 스키마 정규화 → 데이터 검증 → CPA·ROAS·전환율 계산 → 전주 비교 → 로컬 CSV와 Google Sheets 반영`

- 공통 식별자: 날짜·채널·캠페인
- 공통 지표: 비용·노출·클릭·전환·매출
- 계산 원칙: 원천 지표 합산 후 비율 계산
- 품질 원칙: 원본 결측은 0이 아닌 결측으로 유지
- 추적 원칙: 원본 파일·행, VAT 기준, 전환·매출 정의 보존
- 출력 구조: 5개 채널 탭과 별도 `전주비교` 탭

## MVP 범위

- P1 채널 수집: API 연동 자산 부재에 따른 제외
- P2 CSV 정규화·검증·채널별 시트 반영: 구현
- P3 CPA·ROAS·전환율 계산과 제외 사유: 구현
- P4 전주 값·금주 값·증감·증감률 계산: 구현
- P5 주요 변화 및 성과 요약: 사람의 판단이 필요한 보고 영역으로 유지, 구현 제외

제한 시간 2시간 이내에서 객관적 계산과 반복 작업 제거에 집중. 중요도 판단과 성과 맥락 해석은 자동화 범위에서 제외.

## 가정

- 입력은 관리자 화면에서 내려받은 GA4·Meta·네이버 검색광고·네이버 GFA·카카오 모먼트 CSV 5종
- 채널별 전환 정의는 강제 통합하지 않고 원본 정의 유지
- 비용과 매출이 함께 존재하고 정의가 일치하는 경우에만 ROAS 계산
- 0 분모, 필수 값 결측, VAT·전환·매출 정의 혼합은 계산 제외와 사유 표시
- 제공 데이터는 구조 검증용 가상 값이며 사업 성과 판단 근거에서 제외

## 다음 2단계

1. 실제 운영 CSV의 예외 유형 수집과 채널별 매핑 규칙 확장, 실패 행 검토 화면과 운영 이력 관리
2. 채널 API 수집 연동과 P4 비교 결과 기반 요약 초안 보조, 담당자 승인 이후 최종 보고 확정
