# AI 활용 기록

## 활용 범위

AI를 문서 분석, 문제 분해, 설계 검토, 구현, 테스트 작성, 실제 Google Sheets 연동 검증에 활용. 자동 생성 결과는 사용자의 범위 결정과 헤드 에이전트의 통합 검증을 거쳐 채택.

| 단계 | AI 활용 내용 | 결과 |
|---|---|---|
| 과제 분석 | 제공 문서와 CSV 5종 구조 분석, P1~P5 문제 분해 | P2~P4 MVP 범위 확정 |
| 아키텍처 | 헤드 에이전트의 파일트리, 문제별 책임, 데이터 흐름, 공통 설정 계약 설계 | 담당 영역 충돌 방지와 일관된 입출력 구조 |
| P2 | 채널 판별, 공통 스키마, 결측·정의 보존, Google Sheets 출력 설계·구현 | 정규화 154행과 채널별 5개 탭 |
| P3 | CPA·ROAS·전환율 계산과 계산 제외 조건 구현 | 채널·캠페인 11개 그룹 |
| P4 | ISO 주간 전주 비교와 상태 분류 구현 | 8개 지표 기준 88행 |
| 통합 | 환경 설정, Windows 실행파일, 실제 시트 업로드와 재조회 | 6개 탭 242행 반영 |

## 헤드 에이전트의 직접 설계·구현

- 전체 아키텍처: [`docs/architecture/FILE_TREE.md`](docs/architecture/FILE_TREE.md)의 표준 파일 배치와 문제별 소유 경로 설계
- 통합 파이프라인: [`docs/architecture/통합_파이프라인.md`](docs/architecture/통합_파이프라인.md)의 P2→P3→P4→Google Sheets 실행 순서와 실패 경계 설계
- 실행 오케스트레이션: [`src/pipeline.py`](src/pipeline.py)의 설정 로드, 단계별 실행, 임시 결과 검증, 최종 결과 반영, 실행 로그 처리
- 사용자 진입점: [`Sangsaeng_Report_Automation.exe`](Sangsaeng_Report_Automation.exe)와 `scripts/executable_launcher.py`의 더블클릭 실행·성공/실패 대화상자
- 환경 계약: [`.env.example`](.env.example)의 입력·출력·로그·시트 ID·서비스 계정 경로 분리
- 통합 검증: P2·P3·P4 결과 연결, 자동 테스트 48건, 실제 Google Sheets 업로드와 API 재조회
- 제출 문서: [`README.md`](README.md), [`설계메모.md`](설계메모.md), 본 AI 활용 기록의 제출 구조 정리

## 핵심 핸드오프 프롬프트

아래 내용은 헤드 에이전트가 문제 담당 에이전트에게 보낸 최초 인수인계 프롬프트의 요약과 원문 일부 발췌.

### 작업 하네스와 권한 통제

- 역할 하네스: 전체 문제를 P1~P5로 분리하고 P2·P3·P4 담당 에이전트와 통합 담당 헤드 에이전트로 책임 구분
- 읽기 범위: `AGENTS.md`, `docs/architecture/FILE_TREE.md`, 상위 단계 설계·결과 문서, 담당 구현에 필요한 입력·소스만 허용
- 수정 범위: 핸드오프에 지정한 문제별 `src/`, `tests/`, `docs/`, `output/` 하위 경로로 제한
- 충돌 방지: 파일 수정 전 A2A `touch` 등록, 다른 담당 문제의 구현 파일 수정 금지, 공통 모듈 수정은 헤드 에이전트 판단 이후 진행
- 파일트리 하네스: 구현은 `src/`, 테스트는 `tests/`, 문서는 `docs/`, 입력은 `data/input/`, 결과는 `output/`으로 강제
- 반환 계약: 시작·완료 시각, 소요 시간, 구현·제외 범위, 생성·수정 파일, 테스트 명령과 결과를 문제별 한 번의 반환 기록으로 통합
- 검증 게이트: 단위·통합 테스트, 제공 데이터 인수 결과, Google Sheets 업로드 후 API 재조회 일치를 통과한 결과만 채택
- 보안 경계: 서비스 계정 JSON·`.env`·원본 데이터·실행 결과의 저장소 기록과 에이전트 간 전송 금지

### P2. CSV 정규화와 Google Sheets 반영

요약: 로컬 프로그램 내부 정규화, 헤더 기반 채널 판별, 공통 스키마와 결측·정의 보존, 정규화 엔진과 Google Sheets 출력 계층 분리 요청.

권한 범위: `AGENTS.md`와 파일트리 문서 선행 확인. P2 구현·테스트·문서·정규화 결과 경로만 수정. 루트 신규 파일, `AGENTS.md`, 전체 설계 문서의 직접 수정 금지.

```text
[사용자 직접 지시 / P2 인수인계]

당신은 P2 담당 세션.

P2 확정 방향:
1. MCP나 구글시트 내부에서 정규화하지 않고 로컬 프로그램 내부에서 정규화.
2. 입력 폴더의 CSV를 헤더 기반으로 채널 자동 판별.
3. 날짜·채널·캠페인·비용·노출·클릭·전환·매출의 공통 스키마로 변환.
4. 원본에 없는 값은 결측으로 유지. VAT 기준과 전환 정의는 메타데이터로 보존. 의미가 다른 지표의 강제 합산 금지.
5. 정규화 엔진과 구글시트 업로드를 분리. 구글시트는 출력 어댑터로 취급.
6. 목표 흐름: CSV 다운로드 → 실행 → 자동 판별·정규화·검증 → 구글시트 업로드.

요청:
- 위 P2 방향의 설계 검토와 구체화.
- 구현 범위, 제외 범위, 주요 실패 조건, 검증 기준 제안.
```

후속 파일트리 통제 원문 일부:

```text
[사용자 직접 지시 / 파일트리 준수]

루트 신규 파일 생성 중지.
현재 P2 산출물의 표준 경로:
- 구현: src/normalization/normalize_csvs.py
- 정규화 결과: output/normalized/normalized_data.csv
- P2 테스트: tests/normalization/
- P2 문서: docs/P2/

루트 허용 파일은 AGENTS.md, README.md, .gitignore, 프로젝트 설정, 사용자 실행 진입점으로 제한.
이후 작업 전 AGENTS.md와 docs/architecture/FILE_TREE.md 확인.
```

### P3. 파생 지표 계산

요약: 원천 지표 합산 이후 CPA·ROAS·전환율 계산, 0 분모·결측·정의 혼합의 계산 제외, P4 재사용이 가능한 집계 인터페이스 요청.

권한 범위: P2 설계와 파일트리 문서는 읽기 전용. `src/metrics/`, `tests/metrics/`, `docs/P3/`, `output/reports/`만 수정. P2 변경, P4·P5 구현, 루트 파일 생성 금지.

```text
[사용자 직접 지시 / P3 인수인계]

당신은 P3 파생 지표 계산 담당 세션.

작업 전 필수 확인:
- AGENTS.md
- docs/architecture/FILE_TREE.md
- docs/설계.md
- docs/P2/P2_정규화_설계검토.md

P3 확정 방향:
1. P2의 정규화 데이터를 입력으로 사용.
2. CPA = 비용 합계 / 전환 합계.
3. ROAS = 매출 합계 / 비용 합계. 내부 비율값과 사람이 보는 퍼센트 표기의 관계를 명시.
4. 전환율 = 전환 합계 / 클릭 합계.
5. 행별 비율의 평균 금지. 원천 지표를 먼저 합산한 뒤 비율 계산.
6. 분모 0, 필수 값 결측, VAT 기준 혼합, 전환 정의 혼합 시 계산 제외와 사유 출력.

권장 인터페이스:
- 그룹 기준을 입력받아 합계와 파생 지표를 계산하는 재사용 함수.
- 기본 결과는 채널·캠페인 단위.
- P4가 주차 기준으로 재사용할 수 있도록 집계 기준 분리.

파일 경로 제한:
- 구현: src/metrics/
- 테스트: tests/metrics/
- 설계·결과 문서: docs/P3/
- 샘플 실행 결과: output/reports/
- 루트 신규 파일 생성 금지.
```

### P4. 전주 대비 비교

요약: ISO 주간 기준의 전주·금주 비교, 원천 합계 기반 비율 재계산, 예외 상태 분류, 별도 `전주비교` 탭 입력 형식 요청. 해석과 요약 문장은 명시적 제외.

권한 범위: P3 문서와 계산 함수는 읽기·재사용만 허용. `src/comparison/`, `tests/comparison/`, `docs/P4/`, `output/reports/`만 수정. 공통 Google Sheets 어댑터, P2·P3 구현, P5 보고 영역 수정 금지.

```text
[사용자 직접 지시 / P4 인수인계]

당신은 P4 전주 대비 비교 담당 세션.

작업 전 필수 확인:
- AGENTS.md
- docs/architecture/FILE_TREE.md
- docs/설계.md
- docs/P3/P3_파생지표_구현결과.md
- src/metrics/calculate_metrics.py

사용자 결정:
1. MVP는 P4까지 구현.
2. P4 결과는 구글시트의 별도 `전주비교` 탭에 정리.
3. P4는 객관적인 계산과 반복 작업까지만 담당.
4. 좋아짐·나빠짐 판단, 성과 맥락 해석, 요약 문장 작성은 제외.

계산 기준:
- previous_value, current_value
- change = current_value - previous_value
- change_rate = change / previous_value
- 비율 표시는 내부 비율값과 사람이 보는 퍼센트의 관계 명시
- 결측을 0으로 대체 금지

예외 상태:
- 전주 0: 증감률 계산 제외
- 전주 없음·금주 존재: 신규
- 전주 존재·금주 없음: 금주 미집행
- 양쪽 결측: 비교 제외

파일 경로 제한:
- 구현: src/comparison/
- 테스트: tests/comparison/
- 문서: docs/P4/
- 샘플 결과: output/reports/
- 루트 신규 파일 생성 금지

실제 구글시트 인증·업로드 어댑터의 공통 수정은 헤드 에이전트 검토 없이 금지.
```

## 사람의 수정·판단

- CSV의 Google Sheets 반영 자동화와 로컬 정규화 파이프라인을 사용자가 직접 제안하고 최종 방향으로 결정
- P4까지의 객관적 작업만 자동화하고 P5 보고 문장은 사람의 검토 영역으로 남기는 범위 결정
- 영문 필드의 가독성 문제 제기에 따른 Google Sheets 표시 계층 한글화 결정
- 개발자용 명령 실행 방식 거부와 비개발자용 단일 실행파일 전환 결정
- 원본에 없는 값의 임의 0 처리 금지, 결측 유지와 정의 불일치 계산 제외 원칙 채택

## 검증 결과

- 자동 테스트 48건 통과
- 제공 CSV 5종 154행 정규화
- 파생 지표 11개 그룹과 전주 비교 88행 생성
- Google Sheets 6개 탭 242행 업로드와 API 재조회 일치
- 서비스 계정 JSON·`.env`·원본 데이터·실행 결과의 Git 추적 제외

상세 작업 흐름과 문제별 반환 기록은 [`docs/작업_타임라인.md`](docs/작업_타임라인.md) 참조.
